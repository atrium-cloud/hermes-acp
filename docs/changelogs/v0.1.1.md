Fix: model value ids now canonicalize named custom provider entries (`custom:<entry>`) to the `custom` lane slug, so a session reporting `custom:<entry>/<model>` round-trips against the catalog's `custom/<model>` option and `session/set_config_option` echoes match what was sent.
